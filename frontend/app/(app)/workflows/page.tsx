import { PlaceholderPage } from "@/components/placeholder-page";

export default function WorkflowsPage() {
  return (
    <PlaceholderPage
      title="Workflows"
      description="Define how work should move from a trigger to a finished outcome, including approvals."
      emptyTitle="No workflows yet"
      emptyDescription="Workflows will appear here when you design them. Nothing is automated until a workflow is created and switched on."
    />
  );
}
