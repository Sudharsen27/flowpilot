import { PlaceholderPage } from "@/components/placeholder-page";

export default function CommandCenterPage() {
  return (
    <PlaceholderPage
      title="Command Center"
      description="See work that needs attention, what agents are doing, and business outcomes in one place."
      emptyTitle="Nothing to review yet"
      emptyDescription="When agents start handling enquiries, this page will show items that need a decision and results you can measure. There is no activity yet."
    />
  );
}
