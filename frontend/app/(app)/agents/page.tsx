import { PlaceholderPage } from "@/components/placeholder-page";

export default function AgentsPage() {
  return (
    <PlaceholderPage
      title="AI Agents"
      description="Create agents, choose what they can do, and set the permissions they must follow."
      emptyTitle="No agents yet"
      emptyDescription="Agents will appear here once you create them. Each agent will have a clear job, tools, and limits."
    />
  );
}
